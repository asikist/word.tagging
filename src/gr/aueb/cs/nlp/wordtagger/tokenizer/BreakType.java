package gr.aueb.cs.nlp.wordtagger.tokenizer;

/**
 * This enum operator is used to signify how the string 
 * must be split(only the repetition start needs breakAfter).
 * @author Alexandros
 * @since 5/2017
 */

public enum BreakType{
	breakBefore, breakAfter;
}